apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-config-and-start-nfs-ganesha
  labels:
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
stringData:
  config-and-start-nfs-ganesha.sh: |-
    {{ tpl (.Files.Get "files/config-and-start-nfs-ganesha.sh") . | b64enc }}
